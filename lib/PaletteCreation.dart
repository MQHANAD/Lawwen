import 'package:flutter/material.dart';

class Palettecreation extends StatelessWidget {
  const Palettecreation({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Palette'),
    );
  }
}